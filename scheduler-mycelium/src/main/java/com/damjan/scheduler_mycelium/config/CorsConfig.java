package com.damjan.scheduler_mycelium.config;

/**
 * CORS is now configured via {@link SecurityConfig#corsConfigurationSource()}.
 * This class is intentionally empty — do not add a CorsFilter bean here,
 * as it would conflict with Spring Security's built-in CORS integration and
 * result in duplicate Access-Control headers.
 */
public class CorsConfig {
}
